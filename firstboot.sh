#!/bin/bash

# This script is run after the installation of the system, after the first boot.
# Use it to initialise anything that can't be done during installation, such as wallpaper, etc.
# Don't run any "exit" commands in this script, as this script is designed to self-destruct.
# This script is NOT run as root. The username is $USER.

